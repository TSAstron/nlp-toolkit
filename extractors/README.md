As the extraction process is specific to each source and contents, it makes sense to make it interactive - hence the Jupyter noteboooks instead of pure python.
See the specific files, for detailed comments.
